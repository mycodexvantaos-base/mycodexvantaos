# infra/kubernetes/base/mycodexvantaos-core-config/deployment.yaml
# 5.4, 6.4 — metadata.name MUST equal the service-id. Namespace set by overlay only (12.1).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-core-config
  labels:
    app.kubernetes.io/name: mycodexvantaos-core-config
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-core-config
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-core-config
    spec:
      containers:
        - name: mycodexvantaos-core-config
          image: ghcr.io/mycodexvantaos/mycodexvantaos-core-config:latest
          # Production MUST use digest pinning (7.3):
          # image: ghcr.io/mycodexvantaos/mycodexvantaos-core-config@sha256:<digest>
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: mycodexvantaos-core-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
