# infra/kubernetes/base/mycodexvantaos-data-vector-store/deployment.yaml
# 5.4, 6.4 — metadata.name MUST equal the service-id. Namespace set by overlay only (12.1).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-data-vector-store
  labels:
    app.kubernetes.io/name: mycodexvantaos-data-vector-store
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-data-vector-store
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-data-vector-store
    spec:
      containers:
        - name: mycodexvantaos-data-vector-store
          image: ghcr.io/mycodexvantaos/mycodexvantaos-data-vector-store:latest
          # Production MUST use digest pinning (7.3):
          # image: ghcr.io/mycodexvantaos/mycodexvantaos-data-vector-store@sha256:<digest>
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: mycodexvantaos-data-vector-store
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
