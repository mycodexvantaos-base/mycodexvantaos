# infra/kubernetes/base/mycodexvantaos-core-kernel/deployment.yaml
# 5.4, 6.4 — metadata.name MUST equal the service-id. Namespace set by overlay only (12.1).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-core-kernel
  labels:
    app.kubernetes.io/name: mycodexvantaos-core-kernel
    app.kubernetes.io/managed-by: mycodexvantaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mycodexvantaos-core-kernel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mycodexvantaos-core-kernel
    spec:
      containers:
        - name: mycodexvantaos-core-kernel
          image: ghcr.io/mycodexvantaos/mycodexvantaos-core-kernel:latest
          # Production MUST use digest pinning (7.3):
          # image: ghcr.io/mycodexvantaos/mycodexvantaos-core-kernel@sha256:<digest>
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: mycodexvantaos-core-kernel
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
