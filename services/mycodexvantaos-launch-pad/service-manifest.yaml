# service-manifest.yaml
# Service manifest for mycodexvantaos-launch-pad
# Generated automatically by MyCodeXvantaOS governance tooling
# Do not edit manually unless you know what you're doing

apiVersion: mycodexvantaos.org/v1
kind: ServiceManifest
metadata:
  name: mycodexvantaos-launch-pad
  namespace: mycodexvantaos
  labels:
    app.kubernetes.io/name: mycodexvantaos-launch-pad
    app.kubernetes.io/part-of: mycodexvantaos
    app.kubernetes.io/version: v1.0.0
    layer: Deployment
    capability: deploy
  annotations:
    governance.mycodexvantaos.org/spec: platform-governance-spec-v1.0.0
    governance.mycodexvantaos.org/last-audit-date: 2026-05-04T11:33:07Z

spec:
  # Service identification
  serviceName: mycodexvantaos-launch-pad
  serviceVersion: v1.0.0
  
  # Layer and capability mapping
  layer: Deployment
  capability: deploy
  
  # Service configuration
  description: MyCodeXvantaOS mycodexvantaos-launch-pad service
  category: platform
  
  # Health and availability
  healthCheck:
    enabled: true
    endpoint: /health
    interval: 30s
    timeout: 5s
    failureThreshold: 3
  
  # Resource requirements
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  
  # Ports
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  
  # Environment
  env:
    - name: NODE_ENV
      value: "production"
    - name: LOG_LEVEL
      value: "info"
  
  # Dependencies
  dependencies:
    - name: mycodexvantaos-core-kernel
      required: true
    - name: mycodexvantaos-core-config
      required: true
  
  # Security
  security:
    serviceAccountName: mycodexvantaos-launch-pad
    rbac:
      enabled: true
  
  # Observability
  observability:
    metrics:
      enabled: true
      port: 9090
      path: /metrics
    tracing:
      enabled: true
      samplingRate: 0.1
    logging:
      level: info
      format: json
